@model List<Url>
@{
    var user = ViewBag.User as InforceTestTask.Models.User; 
    Layout = null;
}
<!DOCTYPE html>
<html lang="en">
    <body>
    @if(user == null)
    {
        @Html.Partial("~/Views/URL/Topbar.cshtml", Model);
    }else
    {
        @Html.Partial("~/Views/URL/TopbarAuthorized.cshtml", Model);
    }
        @Html.Partial("~/Views/URL/UrlPartial.cshtml", Model);
    </body>
</html>